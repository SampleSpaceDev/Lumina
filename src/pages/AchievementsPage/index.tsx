import { ComponentProps } from "react";

export function AchievementsPage(props: ComponentProps<any>) {
    // const { slug } = useParams();
    // const { success } = undefined;

    return (
        <div>
            <h1>Achievements</h1>
        </div>
    );
}